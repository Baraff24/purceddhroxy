version: '3'

services:
  purceddhroxy:
    build: Dockerfile
    volumes:
      - ./purceddhroxy:/app/purceddhroxy
    restart: always

  proxy:
    build: .
    command:
      -
    ports:
      - "8080:8080"
    depends_on:
      - purceddhroxy
    restart: always