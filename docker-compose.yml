version: '3'

services:
  djangoapp:
    build: .
    command: "./starter.sh"
    ports:
      - "8000:8000"
    restart: always

  proxy:
    build: .
    command: "python proxy/proxy.py"
    environment:
      - DJANGO_WS_URL=ws://django:8000/ws/packets/
    depends_on:
      - djangoapp